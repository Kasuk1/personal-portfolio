import { FC } from 'react';
import { AboutDataSectionFieldType } from '../../../types/about';

export const AboutField: FC<AboutDataSectionFieldType> = ({
  date,
  entityName,
  position,
  functions,
}) => {
  return <div>
    
  </div>;
};
